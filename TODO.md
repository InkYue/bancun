# 待办

> 优先级：P0 阻断上线 / P1 体验改善 / P2 锦上添花

## P0 - 上线前必做

- [ ] **生产环境 HTTPS** — 当前服务是裸 HTTP，密码 / Token / 收款码都没加密。上线前必须前置 Nginx / Caddy 反代加 TLS。
- [ ] **SecretKey 的存储** — `data/state.json` 当前是明文存腾讯云 SecretKey。上线前要不就把文件权限锁到 `chmod 600` + 给运行用户单独 dir，要不就改成读环境变量（`TENCENT_SMS_SECRET_KEY`）。
- [ ] **修改默认 admin 密码** — 服务首次启动会创建 `slug=admin password=bancun-admin` 的默认账号。务必登录后立刻改。
- [ ] **进程守护** — `node server.js` 直跑会跟着终端死。上线前用 systemd / pm2 / docker 包一层。

## P1 - 体验改善

- [ ] **送礼后的庆祝动画** — 当前点亮只切图标态，差一个礼花 / 粒子高潮（之前考虑过 confetti，被砍掉了，可以根据反馈再加）。
- [ ] **员工自己的送礼记录视图** — 现在 employee 角色看不到自己的活动表（只在仪表盘有最近 6 条）。需要一个完整列表 + 时间筛选。
- [ ] **接入真实微信支付（自动确认到账）** — 当前是**荣誉制**：客户点"我已完成支付"就点亮，没有任何到账回调，理论上有人不付钱也能点亮。
  - **当前能做**：上传收款码图片 / 配 `weixin://...` 跳转链接 → 让客户扫码付钱（被动）
  - **当前做不到**：知道客户付了没、付了多少、是否绑定订单
  - **目标**：客户扫**一次性二维码**（每次不同，绑定金额 + 订单号）→ 微信回调到我们服务 → 验签 → 找到对应员工 + 礼物 → 自动点亮并写一条**真实流水**
  - **业务侧需先准备**：
    - 微信支付**商户号**（mch_id，个体户 / 企业均可，需营业执照）
    - **APIv3 密钥** + API 证书（`apiclient_cert.pem` / `apiclient_key.pem`）
    - **HTTPS 公网可达**的回调 URL（线上必须）
  - **架构**：单商户号统一收款，订单号 `out_trade_no` 编码 `{employeeId}-{giftId}-{phone}-{ts}`，回调时解出来归到对应员工。**不需要每个员工独立商户号。**
  - **工作量预估**：3-5 天（统一下单 / 二维码生成 / 回调验签 / 订单状态机 / 失败重试 / 异常对账）
  - **过渡方案（低成本先上）**：把"我已完成支付"按钮改成"上传支付截图"，admin 在后台核单（看截图金额 / 时间）后手动点亮。约 1-2 天。
- [ ] **SMS 厂商抽象** — 目前只支持腾讯云 SMS。如果要切阿里云 / 容联，得改 server.js。可以抽一个 `sendSms(provider, cfg, phone, code)` interface。
- [ ] **送礼排行榜** — 客户视角的"今日 / 本月 Top 10 慷慨者"（手机号掩码展示）。
- [ ] **员工头像默认占位** — 现在没头像就只显示首字，可以接 `gravatar` 或随机 illustration。
- [ ] **后台手机号反查** — admin 想看"13800138000 这个客户给哪些员工送过礼"，目前没有。可以加个手机号搜索（精确匹配）。

## P2 - 锦上添花

- [ ] **临时一次性链接** — 给客户发个有时效的链接（24h / 一次扫码即失效），用完不能再访问。
- [ ] **员工排序 / 标签** — 后台员工列表支持拖拽排序 / 标签分组。
- [ ] **i18n** — 当前全部硬编码中文。
- [ ] **多语言短信模板** — 国际手机号支持 + 不同模板。
- [ ] **送礼月报 PDF** — 每位员工每月生成自己的送礼月报。
- [ ] **暗色模式切换** — 之前做过深色版，可以加个 toggle。
- [ ] **SSE 实时推送** — 当前是 8 / 12 秒轮询，多人同时在场时改 SSE 体验更好。
- [ ] **导入 / 导出员工 CSV** — 一次性创建几十个员工时方便。
- [ ] **数据库选项** — 当 employees > 几百 / activities > 几千时，`state.json` 的全量读写会慢。可以引入 SQLite（仍是单文件）。

## 已知小坑

- [ ] 顶部条 / 侧边栏在极窄屏（< 360px）会有点挤，需要进一步收紧
- [ ] 礼物 GIF 图自定义图加载较慢时无 loading skeleton
- [ ] 客户登录闸门在键盘弹起时位置可能被遮挡（未做特殊处理）

## 可能的产品改造

- [ ] **多店铺 / 工作室** — 当前一个站只有一套礼物目录、一个站点名。要做多店铺得再抽一层 tenant。
- [ ] **送礼私聊 / 留言上墙** — 客户可以附文字（之前曾迭代过 Sweet Wall，被回滚）。是否上线看产品节奏。
- [ ] **粉丝等级 / 积分** — 同一手机号累计送礼多了升级，享受表情 / 头衔 / 通道。
